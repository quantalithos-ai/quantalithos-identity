[package]
name = "identity-domain"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
name = "identity_domain"
path = "src/lib.rs"

[dependencies]
core-contracts.workspace = true
identity-contracts = { path = "../contracts" }
